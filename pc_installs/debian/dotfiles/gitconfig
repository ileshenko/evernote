[user]
    name = Igor Leshenko
    email = jungoigorl@gmail.com
[color]
    branch = auto
    diff = auto
    status = auto
[alias]
    a = add -A
    ci = commit
    d = diff
    dc = diff --cached
    pom = push origin master
    logp = log --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit --

[diff]
	tool = def

[difftool]
	prompt = no

[difftool "def"]
	cmd = $DIFFPROG $LOCAL $REMOTE
