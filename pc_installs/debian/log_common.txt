git clone https://github.com/ileshenko/evernote.git evernote_git
cd
ln -s ~/evernote_git/pc_installs/debian/home_bin/ ~/bin

cd ~/evernote_git/pc_installs/debian/dotfiles
for i in * ; do ln -v -s `pwd`$i ~/.$i ; done

cd /bin
sudo ln -sf bash sh

.bashrc:
#-----------------------------------------------
uncomment "force_color_prompt=yes"
add
if [ -f ~/.bashrc_igor ]; then
    .  ~/.bashrc_igor
fi
#-----------------------------------------------

cd evernote_git/pc_installs/debian/home_bin
rt chown root:root lpt_pwr
rt chmod +s lpt_pwr


TODO: 
# Explicit rule for exec_as_root - don't use external dependencies
exec_as_root: exec_as_root.c
	$(CC) -o $@ $<

install_rt: exec_as_root
ifneq ($(OS),Windows_NT)
	echo "Please enter root's password (needed by rt - exec_as_root):"
	su root -c "install -m 4755 -o root -g root exec_as_root /usr/bin/rt"
else
	install -m 4755 exec_as_root /usr/bin/rt
endif


